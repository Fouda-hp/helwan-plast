import anvil.users
import anvil.files
from anvil.files import data_files
"""
access_policy.py - توحيد سياسة الوصول (Enterprise / SaaS)
==========================================================
يُستخدم لتوثيق سبب كون بعض الدوال عامة أو تتطلب مصادقة أو أدمن فقط.
لا يغيّر سلوك الكود الحالي؛ للتوثيق والمراجعة الأمنية فقط.
"""

# ========== أنماط الوصول ==========
# PUBLIC_READ_ONLY: يمكن استدعاؤها بدون مصادقة؛ قراءة فقط ولا تعدّل بيانات حساسة.
# السبب: تحتاجها واجهات التحميل الأولي (مثل الحصول على الرقم التالي للعميل/العرض) أو إعدادات القراءة العامة.
PUBLIC_READ_ONLY = "PUBLIC_READ_ONLY"

# AUTHENTICATED_READ: تتطلب مستخدماً مسجّل الدخول (أي دور ما عدا عدم التسجيل).
# السبب: عرض بيانات تعتمد على الصلاحيات (عروض، عملاء، تصدير، إلخ).
AUTHENTICATED_READ = "AUTHENTICATED_READ"

# ADMIN_ONLY: تتطلب دور مدير نظام (admin).
# السبب: إدارة المستخدمين، الإعدادات، النسخ الاحتياطي، سجل التدقيق، استيراد CSV.
ADMIN_ONLY = "ADMIN_ONLY"


# ========== توثيق الدوال العامة (للمراجعة فقط) ==========
# الدوال التالية مُعرّفة كـ PUBLIC_READ_ONLY عن قصد:
#
# - get_next_client_code / get_next_quotation_number: تستدعي get_next_number_atomic (عداد ذرّي)؛ قراءة وتحديث عدّاد فقط.
# - get_next_number_atomic: قراءة وتحديث جدول counters بشكل ذرّي؛ لا تُرجع بيانات مستخدمين.
# - get_setting(key): قراءة إعداد واحد؛ قد تُستخدم من واجهات عامة (مثل سعر الصرف للعرض).
# - get_machine_prices(): قراءة أسعار المكن؛ للعرض في الحاسبة.
# - get_calculator_settings: في الواقع تتطلب مصادقة (admin/manager) في التنفيذ الحالي.
#
# إن رغبت بتقييد أي من الدوال أعلاه لمستخدم مسجّل فقط، أضف تحقق مصادقة في الدالة
# وحدّث هذا الملف ليعكس النمط الجديد.
